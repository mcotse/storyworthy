# Supabase Configuration
# Get these from: https://supabase.com/dashboard/project/YOUR_PROJECT/settings/api
# Use the new Publishable key (starts with sb_publishable_) instead of the legacy anon key
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_your-key-here

# Push Notifications (optional)
# VAPID key pair: generate with `npx web-push generate-vapid-keys`
VITE_VAPID_PUBLIC_KEY=your-vapid-public-key-here
# URL of the Cloudflare Worker that handles push subscriptions
VITE_PUSH_WORKER_URL=https://your-worker.your-subdomain.workers.dev
